# UI for CousCous a Colazione website

UI for CousCous a Colazione website built in react with vite.

### Run manually
`npm run start`

### Run with Docker
Run from the repository root (Caddy handles HTTPS and Let’s Encrypt):
`docker compose up -d --build`